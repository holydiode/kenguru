@model KenguruDB
@{
    ViewBag.Title = "Index";
}
@section links{
    <link rel="stylesheet" href="~/Content/Home.css">
}

<div class="left-menu">
    <div class="categories">
        @{
            foreach (Category category in Model.Categories.ToList())
            {
                if (category.parent == null)
                {
                    <div id="dropdown-item" class="category">
                        <a class="category-link" href="@Url.Action("Index", "Catalog", new { categoryId = category.id, sortBy = 1 })">@category.name</a>
                        @if (category.subCategories.Count != 0)
                        {
                            <div id="submenu">
                                @foreach (Category subCategory in category.subCategories)
                                {
                                    <div class="category">
                                        <a class="category-link" href="@Url.Action("Index", "Catalog", new { categoryId = subCategory.id, sortBy = 1 })">@subCategory.name</a>
                                    </div>
                                }
                            </div>
                        }
                    </div>
                }
            }
        }

    </div>
</div>
<div class="home-content">
    <div class="blocks">
        <a class="block-link" href="@Url.Action("CheckStateOrder")">
            <div class="avatar">
                <img src="~/UploadFiles/Home/Search.png" />
            </div>
            <div class="title">Отследить заказ по трек-номеру</div>
        </a>
        <a class="block-link" href="@Url.Action("Index", "Seller")">
            <div class="avatar">
                <img src="~/UploadFiles/Home/seller.png" />
            </div>
            <div class="title">Личный кабинет продавца</div>
        </a>
        <div class="block-link">

        </div>
    </div>
</div>